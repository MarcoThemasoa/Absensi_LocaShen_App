# Absensi Digital GeoFace

Sistem absensi berbasis face recognition dengan geolocation.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set `.env` variables (lihat `.env.example`)
3. Run the app:
   `npm run dev`
